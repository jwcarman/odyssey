LAST_RUN: 2026-04-11T12:00:00-04:00
ITERATION: 48
SPEC_COMPLETED: 045-odyssey-api-redesign.md
WHAT_WAS_DONE: Redesigned Odyssey's public API around typed events, customizer-based configuration, and producer/consumer split. Replaced OdysseyStream/OdysseyStreamRegistry/OdysseyEvent with Odyssey facade, OdysseyPublisher<T>, PublisherConfig, SubscriberConfig<T>, DeliveredEvent<T>, generic SseEventMapper<T> with terminal hooks, and SseJournalAdapter<T>. Added PublisherCustomizer/SubscriberCustomizer beans, createOrConnect cluster-safe provisioning, and rich terminal-state SSE events.
VERIFICATION: ./mvnw clean verify → BUILD SUCCESS (46 tests, 0 failures); ./mvnw spotless:check → BUILD SUCCESS
STATUS: DONE
NEXT_SPEC: none
NOTES: StoredEvent is non-generic with String data field (pre-serialized JSON) to avoid TypeRef complexity with generic journal types — the spec's TypeRef suggestion was verified and found impractical due to type erasure. ObjectMapper is used in publisher (serialize T→JSON), adapter (deserialize JSON→T for DeliveredEvent<T>), and default SseEventMapper (serialize T for SSE data field). SubscriberCustomizer uses option (b) from the spec: Consumer<SubscriberConfig<?>>, which naturally prevents global customizers from setting the typed mapper. The NullPointerException logs in writer threads during DefaultOdysseyTest are harmless — they occur because mock journals return null for subscribe() calls that happen lazily in background threads, but the tests verify facade behavior correctly. Backend starter modules are unchanged.
