LAST_RUN: 2026-04-06T12:17:00-04:00
ITERATION: 9
SPEC_COMPLETED: 009-example-app.md
WHAT_WAS_DONE: Created odyssey-example Spring Boot application module with three REST controllers (BroadcastController, NotifyController, TaskController) demonstrating all three stream types, a static HTML demo page with EventSource subscriptions, application.yml config, and a docker-compose.yml for Redis. The example depends solely on odyssey-spring-boot-starter.
VERIFICATION: ./mvnw clean verify → BUILD SUCCESS (64 tests, 0 failures); ./mvnw spotless:check → BUILD SUCCESS (4 files clean)
STATUS: MORE_WORK
NEXT_SPEC: 010-integration-tests.md
NOTES: TaskController stores ephemeral stream references in a ConcurrentHashMap keyed by streamKey, since registry.ephemeral() generates a new stream each call and there's no API to retrieve an existing stream by key. The map entry is cleaned up when the task completes and calls stream.close(). The HTML uses named event listeners (e.g., addEventListener('message'), addEventListener('notification'), addEventListener('progress'/'complete')) matching the event types published by each controller.
