LAST_RUN: 2026-04-06T14:37:00-04:00
ITERATION: 23
SPEC_COMPLETED: 020-notifier-postgresql.md
WHAT_WAS_DONE: Created odyssey-notifier-postgresql module — an OdysseyStreamNotifier implementation backed by PostgreSQL LISTEN/NOTIFY, with a dedicated virtual thread holding a raw JDBC connection for LISTEN, SmartLifecycle start/stop, client-side notification dispatch via a single channel, auto-configuration gated on @ConditionalOnProperty(odyssey.notifier.type=postgresql), and Testcontainers-based integration tests.
VERIFICATION: ./mvnw clean verify → BUILD SUCCESS (all modules, 0 failures); ./mvnw spotless:check → BUILD SUCCESS
STATUS: MORE_WORK
NEXT_SPEC: 021-eventlog-cassandra.md
NOTES: Uses a single PostgreSQL channel (odyssey_notify) for all notifications — payload format is "streamKey|eventId". The LISTEN connection is a dedicated long-lived raw JDBC connection obtained from the DataSource (not pooled). Reconnect logic with 1-second backoff if the connection drops. The notify() method uses JdbcTemplate with string-escaped pg_notify calls through pooled connections. Poll timeout is configurable via odyssey.notifier.postgresql.poll-timeout-millis (default 500ms). PostgreSQL driver is a compile dependency since PGConnection is needed for getNotifications().
