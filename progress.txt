LAST_RUN: 2026-04-06T15:57:00-04:00
ITERATION: 30
SPEC_COMPLETED: 027-eventlog-rabbitmq.md
WHAT_WAS_DONE: Created odyssey-eventlog-rabbitmq module — a RabbitMqOdysseyEventLog implementation using RabbitMQ Streams (com.rabbitmq:stream-client). append() creates streams on first use with configurable maxAge and maxLengthBytes retention, publishes messages with event metadata as AMQP 1.0 application properties and payload as message body, returns timestamp-sequence event IDs (013d-05d format). readAfter() consumes all messages from the stream via a blocking queue with timeout, filters by eventId > lastId. readLast() consumes all and takes last N. delete() closes the cached producer and deletes the stream. Auto-configuration gated on @ConditionalOnClass(Environment.class). Integration tests with Testcontainers RabbitMQ (stream plugin enabled via withCopyToContainer for enabled_plugins file).
VERIFICATION: ./mvnw clean verify → BUILD SUCCESS (all modules, 0 failures, 15 RabbitMQ tests passed — 13 event log ITs + 2 auto-config ITs); ./mvnw spotless:check → BUILD SUCCESS
STATUS: MORE_WORK
NEXT_SPEC: 028-eventlog-nats.md
NOTES: Uses com.rabbitmq:stream-client (version managed by Spring Boot). Timestamp-based event IDs (same format as MongoDB/DynamoDB modules) rather than native stream offsets, because the stream client doesn't expose the server-assigned offset to the producer. Read operations consume all messages from the stream with a 200ms idle timeout — acceptable since streams are bounded by retention (maxAge/maxLengthBytes). Testcontainers 2.x RabbitMQContainer lacks withPluginsEnabled() — used withCopyToContainer(Transferable.of("[rabbitmq_management,rabbitmq_stream]."), "/etc/rabbitmq/enabled_plugins") instead. Parent pom.xml and odyssey-bom updated with the new module.
