LAST_RUN: 2026-04-06T16:09:00-04:00
ITERATION: 31
SPEC_COMPLETED: 028-eventlog-nats.md
WHAT_WAS_DONE: Created odyssey-eventlog-nats module — a NatsOdysseyEventLog implementation using NATS JetStream. append() publishes messages with event metadata as NATS headers and payload as message body, returns JetStream sequence number as event ID. readAfter() creates an ephemeral pull consumer starting at lastId+1 filtered by subject. readLast() fetches all messages for the subject and trims to last N client-side. delete() purges messages for the stream key's subject via PurgeOptions.subject(). JetStream stream created on startup covering subjects odyssey.events.> with configurable maxAge and maxMessages retention. Auto-configuration gated on @ConditionalOnClass(Connection.class) with @ConditionalOnMissingBean for the NATS Connection bean (shared with odyssey-notifier-nats).
VERIFICATION: ./mvnw clean verify → BUILD SUCCESS (all modules, 0 failures, 15 NATS tests passed — 13 event log ITs + 2 auto-config ITs); ./mvnw spotless:check → BUILD SUCCESS
STATUS: DONE
NEXT_SPEC: none
NOTES: Uses io.nats:jnats 2.20.6 (same version as odyssey-notifier-nats). Subject mapping: streamKey colons replaced with dots (e.g., channel:orders → odyssey.events.channel.orders). JetStream sequence numbers are per-stream not per-subject, so sequences have gaps when filtering by subject — this is fine since readAfter filters by subject and sequences remain ordered. The NATS Connection bean uses @ConditionalOnMissingBean so it's shared when both odyssey-eventlog-nats and odyssey-notifier-nats are on the classpath. Tests use a single stream name with deleteStream in @AfterEach because JetStream enforces unique subject-to-stream mapping (multiple streams can't cover overlapping subjects). Parent pom.xml and odyssey-bom updated with the new module.
